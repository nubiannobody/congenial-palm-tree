function halve(n: number): number {
    return n / 'two';
}

halve(2);

// type error: The right hand side of an arithmetic 
// operation must be of type 'any', 'number', 'bigint' 
// or an enum type.